const express = require('express');
const router = express.Router();

const Cupcake = require('../modles/cupdake');
const mongoose = require('mongoose');

app.get('/', (req,res) =>{
 
})

module.exports = router